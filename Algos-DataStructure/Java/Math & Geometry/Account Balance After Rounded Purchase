class Solution {
    public int accountBalanceAfterPurchase(int purchaseAmount) {
        int balance = 100;
        int roundedAmount = ((purchaseAmount + 5) / 10) * 10;

        return Math.abs(balance - roundedAmount);
    }
}